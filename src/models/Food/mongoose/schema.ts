const foodSchema = {
  title: String,
  callories: Number,
  harmfulness: Number,
  complex: Boolean,
  ingridients: [String],
  groupId: String,
  userId: String,
  descr: String,
  createdAt: String
};

export default foodSchema;