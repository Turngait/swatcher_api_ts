const foodSchema = {
  title: String,
  callories: Number,
  harmfulness: Number,
  complex: Boolean,
  ingridients: [String],
  units: String,
  groupId: String,
  userId: String,
  descr: String,
  createdAt: String
};

export default foodSchema;