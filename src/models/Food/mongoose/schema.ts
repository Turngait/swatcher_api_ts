const foodSchema = {
  title: String,
  callories: Number,
  harmfulness: Number,
  ingridients: [String],
  isComplex: Boolean,
  units: String,
  groupId: String,
  userId: String,
  descr: String,
  createdAt: String
};

export default foodSchema;